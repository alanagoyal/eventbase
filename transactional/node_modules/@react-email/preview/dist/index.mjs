// src/preview.tsx
import * as React from "react";
import { jsx, jsxs } from "react/jsx-runtime";
var Preview = React.forwardRef(
  ({ children = "", ...props }, forwardedRef) => /* @__PURE__ */ jsxs(
    "div",
    {
      ref: forwardedRef,
      id: "__react-email-preview",
      style: {
        display: "none",
        overflow: "hidden",
        lineHeight: "1px",
        opacity: 0,
        maxHeight: 0,
        maxWidth: 0
      },
      ...props,
      children: [
        children,
        renderWhiteSpace(children)
      ]
    }
  )
);
var renderWhiteSpace = (text) => {
  const whiteSpaceCodes = "\xA0\u200C\u200B\u200D\u200E\u200F\uFEFF";
  return /* @__PURE__ */ jsx("div", { children: whiteSpaceCodes.repeat(150 - text.length) });
};
Preview.displayName = "Preview";
export {
  Preview
};
